{
  "name": "@workspace/github-worker",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "npm run types:generate && tsc --noEmit",
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "types:generate": "wrangler types"
  },
  "dependencies": {
    "@workspace/github": "*",
    "@workspace/logger": "*"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "typescript": "^7.0.2",
    "wrangler": "^4.123.0"
  }
}
